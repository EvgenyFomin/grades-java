syntax = "proto3";
import "PersonComment.proto";
option java_multiple_files = true;
package ru.protei.grpc;

service UnidirectionalCommentService {
  rpc getPerson(PersonRequest) returns (stream PersonResponse);
}